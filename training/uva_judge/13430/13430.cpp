#include <bits/stdc++.h>
using namespace std;

const int mxN = 1e3;
string in, line[mxN];
vector<int> adj[mxN];
map<char, int> node_id;
map<int, char> node_name;
bool vis[mxN];

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    while(cin >> in){}
    
    return 0;
}
